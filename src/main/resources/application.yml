spring:
  datasource:
    url: jdbc:mysql://${AIVEN_MYSQL_HOST:localhost}:${AIVEN_MYSQL_PORT:3306}/${AIVEN_MYSQL_DBNAME:e-commerce}?useSSL=false
    username: ${AIVEN_MYSQL_USERNAME:root}
    password: ${AIVEN_MYSQL_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ngochuymail25@gmail.com
    password: pqzy sybn nnim quji
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: "*"
        debug: false
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            redirect-uri: http://localhost:5173/oauth2/callback

file:
  upload-dir: ./uploads/images
  base-url: /image

api:
  prefix: api/v1

jwt:
  secret: EQOIAUgMdB/GxPmYT+uew/HLvOkYDOrAo3ihcFynD+Y=
  expiration: 2592000
  expiration-refresh-token: 5184000 #60 days = 60 * 24 * 60 * 60